import 'package:flutter/material.dart';

class BicycleContent extends StatelessWidget {
  const BicycleContent({super.key});

  @override
  Widget build(BuildContext context){
    return const Icon(Icons.directions_bike);
  }
}